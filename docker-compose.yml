version: '3.8'

services:
  honeypot:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - API_KEY=gov-api-key-2024
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - honeypot
    restart: unless-stopped

volumes:
  data:
  logs: